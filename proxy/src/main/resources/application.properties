server.port=${PORT:8080}
spring.application.name=university-proxy

backend.url=${BACKEND_URL:http://backend:3001}
frontend.url=${FRONTEND_URL:http://frontend:3000}

# Logging
logging.level.com.university.proxy=INFO
logging.level.org.springframework.web=INFO

